<?php

global $page;
global $pdo;

function site_start($uri)
{
    // global $pdo;
    // global $page;

    // //$page = get_page($uri);

    // load_site_template();
}


//contorler jest to plik lub klasa, która odpowiada za przechwycenie adresu url i wyrenderowanie odpowiedniej templatki